# Plugins

This folder is empty in git. Plugins are installed at runtime from `plugin_source`
with `.plugin -add NAME` / `.plugin -update`.

The source of truth is the DLC repo (git URL or a local checkout set via
`.plugin -source`). Do not commit plugin copies here.
